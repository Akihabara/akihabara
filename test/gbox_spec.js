describe("AkihabaraGamebox", function () {
	it("should determine the correct animation frame to display", function () {
		expect(AkihabaraGamebox.decideFrame).toEqual("Implement this test");
	});

	it("should determine the correct animation frame to display just once", function () {
		expect(AkihabaraGamebox.decideFrameOnce).toEqual("Implement this test");
	});

	it("should determine the correct animation frame to display", function () {
		expect(AkihabaraGamebox.isLastFrameOnce).toEqual("Implement this test");
	});
});
